#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    cout << n << "\n";
    for (int i = 0; i < n; ++i) {
        int x, y;
        cin >> x >> y;
        cout << -y << " " << -x << "\n";
    }
}
